{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Unit test",
            "type": "shell",
            "command": "make -j`nproc` unittest OPTIMIZATION=-O0"
        },
        {
            "label": "Demos",
            "type": "shell",
            "command": "make -j`nproc` demos OPTIMIZATION=-O0"
        }
    ]
}
